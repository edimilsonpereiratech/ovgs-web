services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_URL: /api
        NEXT_PUBLIC_ENABLE_MSW: 'true'
    image: ovgs-web:local
    ports:
      - '3000:3000'
    restart: unless-stopped
